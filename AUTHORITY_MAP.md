# Forge semantic authority map

Candidate scope: Architecture Simplification Pass frozen from canonical commit `9bf8d4ec85420b9433b243895f8f86ddfea972c8` / tree `768331f76d33fa00249f42b6d5bd6c0e02470aa8`, developed only in the isolated offline sandbox until final patch reconstruction and local acceptance.

This map records **semantic authority**, not merely file ownership. A facade may translate or orchestrate calls into an authority, but must not independently reimplement admission, persistence, or lifecycle policy. Compatibility paths are acceptable only when they delegate to the canonical writer or are read/import-only.

| Semantic entity | Canonical writer / admission | Persistence owner | Lifecycle owner | Facade / transport boundary | Compatibility boundary | Recovery / reconciliation owner | Second authority after candidate? |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Requirement | `src/runtime/control-plane/facade/requirement-authority.ts` (`admitRequirement`) for bootstrap create/reuse/conflict; completion/update primitives remain in Requirement domain store | `src/runtime/control-plane/persistence/requirement-store.ts` | Requirement store state transitions plus Work-derived completion | `src/runtime/gateway/mcp/runtime-tools.ts` translates typed request/result and no longer performs bootstrap comparison/create policy | Existing callers may use store primitives for already-authorized lifecycle operations; MCP bootstrap does not | Requirement completion is reconciled from accepted Work evidence by `completeRequirementFromWork` | **No.** Bootstrap admission policy has one authority; persistence remains one store. |
| Plan | `src/runtime/control-plane/facade/plan-contract-store.ts` (`admitPlanContract`, draft repair, step claims) | `plan-contract-store.ts` | Same Plan domain module | MCP/runtime facades invoke Plan APIs | Legacy malformed draft repair is an in-place domain operation, not replacement Plan authority | `repairDraftPlanContract`, `repairDanglingPlanStepWorkBinding` | **No source evidence of a second Plan store/writer.** |
| Work | Repository/Goal admission remains canonical; `work-preparation-service.ts` owns prepared Work/managed-workspace/adopt-successor application transactions, `work-operation-service.ts` owns Work command execution/validation, `work-state-machine.ts` owns legal Work transitions, `work-completion-authority.ts` owns terminal completion projection, and `work-verification-service.ts` owns Work verification execution/evidence | `work-contract-store.ts` is the locked WorkContract persistence owner | Pure state machine + canonical completion/verification application services; persistence applies policy but does not define another lifecycle | MCP Runtime, Execution tools, Durable Worker and Local Bridge translate requests/results and call canonical services; control-plane application code never imports Gateway transport | Direct Edit / retained-cancelled compatibility delegates to canonical admission/resume; legacy Issue/Task projection cannot own Work state | Stale Work reconciliation and retained-work recovery use the same WorkContract/state-machine authority | **No.** Persistence, transition policy, verification and completion each have one explicit owner; transports no longer reimplement those semantics. |
| Work verification | `src/runtime/control-plane/execution/work-verification-service.ts` | Process Runtime check records + Work verification records in canonical stores | Resolves the Work checkout/check registry, executes/reuses the Process check, validates receipt/source/workspace identity, classifies result and records Work evidence | `rh_work verify` and Local Bridge are callers only | Legacy Task verification may use Process Runtime only before Issue/Task cutover; post-cutover Work verification is canonical | Invalid/missing Work checkout or receipt identity fails closed as infrastructure evidence | **No.** Candidate/main registry drift cannot be reintroduced by transport-specific lookup. |
| WorkHandle | `src/runtime/control-plane/execution/work-handle-authority.ts` for missing-handle compatibility repair/rebind; `work-preparation-service.ts` owns normal creation/adoption and execution/finalization owns subsequent transitions | `src/runtime/control-plane/execution/work-handle-store.ts` | Work execution/finalization lifecycle; `transitionWorkHandle`/transactional writes | MCP execution facade orchestrates the unique finalizer; runtime facade no longer reconstructs/writes handles directly | Missing-handle compatibility repair is centralized and requires an existing admitted WorkContract | Terminal cleanup/reconciliation in execution subsystem and `work-terminal-cleanup.ts` | **No second WorkHandle store.** |
| Edit Session | `src/cli/editing/edit-session.ts` (`beginEditSession`, `applyEditOperations`, savepoint, verify, rollback, finalize) | Repo-local edit-session persistence owned by `edit-session.ts` | Same edit-session module, fenced by durable Work/checkout binding | MCP legacy/edit adapters call Edit Session APIs | Legacy `begin_edit_session` can request compatibility Direct Edit Work only through canonical repository admission | `revalidateEditSessionOwnership`, `reconcileEditSession`, `reconcileOpenEditSessions`, cleanup | **No.** Legacy transport no longer creates its own WorkContract policy. |
| Finalize | `src/runtime/control-plane/execution/work-finalization-service.ts::finalizeWork` is the single Work delivery/finalization application service; shared identity/Git/cleanup primitives live in `work-execution-support.ts` | WorkHandle + WorkContract canonical stores | Finalization service stages validation/commit/merge/cleanup/receipt and delegates terminal resource cleanup to canonical cleanup authority | `src/runtime/gateway/mcp/execution-tools.ts` only decodes `work_finalize`, calls the service and encodes the result | No separate legacy finalizer; compatibility callers must enter the same Work/Execution authorities | Retryable finalization stages and terminal reconciliation live in the application service/support layer | **No.** Finalization no longer physically lives in MCP transport and there is one application-service owner. |
| Process | Process Runtime APIs and execution authority under `src/runtime/execution/process-runtime/` | `src/runtime/execution/process-runtime/store.ts` (durable records/request bindings/recovery index) | Process Runtime runner/manager + store state transitions | Repository/MCP commands delegate effectful long operations to Process Runtime | Lightweight/direct execution is restricted by operation semantics; effectful Work-attributed commands retain durable Process authority | recoverable records and Process Runtime reconciliation/cleanup | **No second durable Process authority found.** |
| Schedule / Occurrence | Schedule store mutations + scheduler execution contract | `src/runtime/workflow/schedules/store.ts` | Scheduler engine consumes current revision and occurrence state; store owns CAS/persistence | MCP/scheduler surfaces translate requests and execute occurrences | Historical occurrence evidence is retained; compatibility does not own a second schedule store | Scheduler maintenance/reconciliation; store dedupe is explicit and history-preserving | **No.** Scheduler engine is executor, not a parallel persistence authority. |
| Controller Session / Relay | `controller-session-store.ts` owns Work controller claim/release generations; `controller-round-relay.ts` owns semantic round relay/disposition state | Controller session store plus control-plane SQLite namespace `controller_round_relay` | Claim/release generation fence + relay `pending_release/dispatching/dispatched/claimed/...` lifecycle | Runtime/scheduler call `begin...`, `finish...`, `acknowledge...`, disposition APIs | `controller.disposition:*` is a parsed compatibility transport that resolves to canonical relay operations | `claimStalledControllerRoundRelays`, abandoned-release reconciliation, exact-session/runtime migration fences | **No second relay lifecycle found.** Dispatched/claimed rounds remain durable until exact claim/disposition or bounded recovery. |
| BrowserSession / native tab | `browser-session-authority.ts` is the production session writer; `browser-session-store.ts` is the authority-selecting facade; Browser Runtime transaction policy owns replay safety; native session lifecycle owns exact native liveness/reconciliation | Control-plane SQLite Browser session namespace; JSON is import/no-controller compatibility only | Browser Runtime + native lifecycle service; provider bridges execute capabilities but do not own Forge session semantics | `browser-adapter.ts` is composition/action boundary; manifest/schema is separate; macOS/node bridges remain provider transports | Handoff sidecar writes InteractionSession result only and must not write BrowserSession; Runtime projects terminal handoff state back through the canonical session facade | Exact-ref stale/replacement reconciliation in Browser domain/lifecycle services | **No.** Sidecar JSON dual-write and adapter-local replay policy were removed. |
| Runtime release | Typed manifest/materialization + release store remain release identity/publication authority; standalone Recovery owns the transition transaction that composes stop -> publish/rollback authority -> rebind service contract -> start -> whole-Runtime verify | `release-store.ts` plus immutable release manifests/attestations | Shared stop/rebind-start-verify primitives in standalone Recovery prevent callers from freely composing partial release transitions | CLI/Recovery entrypoints call the same transition primitives; ordinary Runtime does not recover itself | Source/package compatibility forms may be staged only through the typed release boundary | Failed activation restores previous release/SQLite then rebinds and verifies before accepting recovery | **No.** Release authority and OS service contract are transitioned as one fenced lifecycle rather than independent caller steps. |
| Recovery | Standalone Recovery remains the independent recovery authority | Recovery/runtime authority and audit state below ControllerHome | `standalone-recovery/core.ts` owns guarded stop, rollback/activation, launch-contract rebinding, restart and whole-Runtime verification transactions | Recovery CLI/MCP expose the authority; canonical Runtime is not a second recovery writer | Compatibility entrypoints delegate to the same core transition service | Recovery itself; failure after release-authority change leaves the service stopped or restores/validates the previous whole release | **No.** Independence is preserved while duplicated caller orchestration was collapsed. |
| Context evidence state | Current repository raw source + bounded relationship expansion (`context-pack.ts`) + CodeGraph/semantic providers are evidence authority for a request | Request-local evidence state; session read cache is derivative; `controller-context.ts` stores derivative projections keyed/fenced by source identity | `buildControllerContextPack` evidence waves/readiness closure; projection refresh lifecycle is separate derivative maintenance | `rh_context`/controller facade returns evidence and machine-readable readiness | Cached/projection data may accelerate lookup but cannot override current source identity/freshness | Coverage gaps, structural freshness, unresolved material paths and projection invalidation/refresh | **No second source authority.** Unresolved discovered material relationships now force `insufficient`. |

## Candidate-specific authority fences

1. **Typed workspace placement is admission data.** `workspaceMode`, `requireWorktree`, and `directMainProhibited` are normalized by `workspace-admission.ts` before route selection. Route Policy or `force_mode=direct_control` cannot silently override explicit isolation.
2. **Requirement bootstrap policy is no longer an MCP concern.** `runtime-tools.ts` translates the request and calls `admitRequirement`; create/reuse/conflict identity semantics live in one domain authority.
3. **Repository compatibility writers delegate.** Direct Edit Work creation, prepared Work creation, Work placement materialization, missing WorkHandle repair, and retained-cancelled Work reauthorization have canonical control-plane entry points rather than transport-owned policy.
4. **Candidate self-host checks have an explicit isolated ControllerHome.** The isolated state root lives under the verification clone's `.git/forge-candidate-controller`, so it is separated from known-good A and does not appear as Candidate source/untracked evidence. Host `FORGE_CONTROLLER_*`/runtime-private state remains sanitized from generic repository child processes.
5. **Compiled Candidate manifests are closed over their executable surface.** Candidate staging reloads the immutable manifest through `release-manifest.ts`, requires every compiled runtime component identity pair, reconciles receipt/release/source identity, then validates physical regular-file/directory type, SHA-256 and execute bits. Unknown future manifest fields remain forward-compatible, but cannot substitute for the known closed component set.
6. **Browser retries are outcome-aware.** Only replay-safe observation actions on existing resources may retry after dispatch. A transient failure after a potentially mutating dispatch returns `PLUGIN_BROWSER_MUTATION_OUTCOME_UNKNOWN` rather than blindly replaying.
7. **Physical distribution copies are not authoring authorities.** Root `scripts/`, root reference configs, and root Claude templates are authoring inputs; packaged downstream copies are deterministic projections checked by `scripts/sync-generated-authority.mjs` and the `prepack` drift gate.
8. **Work verification resolves check authority from the Work-bound checkout.** `rh_work verify` resolves the WorkContract checkout before check-id classification and enumerates that checkout's registered checks. If the Work checkout cannot be resolved, verification fails as infrastructure instead of silently falling back to canonical/main. Candidate-added checks therefore share one revision-bound registry view with Context/Process verification.
