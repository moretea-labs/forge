# Pre-Stable defect disposition

Baseline source commit declared by the freeze: `7c789af8f873370ff8d06c467f8cad679e9c0984`.
Baseline source tree independently reconstructed before modification: `d2db510954aaccc3429d774baee1b6ae9d782e2a`.

The classifications below use the required vocabulary. Historical Work/Handoff text is investigation evidence only; current frozen source behavior is the authority.

| Finding | Disposition | Candidate evidence / rationale | Remaining acceptance |
| --- | --- | --- | --- |
| A — Work path-scope / Direct Edit scope authority | **already fixed / subsumed** | Frozen source already contains fail-closed Work/Edit scope and checkout fences. Candidate preserves them and routes legacy Direct Edit Work admission through `repository-work-admission.ts` rather than adding a new writer. | Normal integration regression suite. |
| A — malformed draft Plan repair | **already fixed / subsumed** | `plan-contract-store.ts::repairDraftPlanContract` remains the canonical in-place repair path; no replacement Plan authority introduced. | None source-specific. |
| A — Schedule revision CAS / current-authority transforms | **already fixed / subsumed** | Frozen `schedules/store.ts` owns revisioned mutations; `control-plane-hardening.test.ts` covers preserving newer human pause/config state during settlement. | Normal scheduler integration regression. |
| A — terminal Work historical attribution / legacy exact-ID cleanup | **already fixed / subsumed** | Existing WorkContract/WorkHandle terminalization and compatibility cleanup retained; candidate centralizes additional missing-handle repair instead of duplicating persistence. | Normal integration regression. |
| A — Controller session generation fencing | **already fixed / subsumed** | `controller-session-store.ts` remains canonical for claim/resume/release generation fencing. | Runtime-rotation live canary in integration. |
| A — previous single-owner convergence P1–P4 | **already fixed / subsumed** | Whole-repo second pass found no second Requirement/Plan/Work/Browser/Process persistence store. Candidate removes additional transport-owned admission/resume/handle/placement policy. | Exact-content gates on integrated tree. |
| A — Browser native replacement provenance/stale-tab repair (`c796dc7c`, `2ddb3231`) | **already fixed / subsumed** | Existing BrowserSessionAuthority/native replacement tests and provenance code are preserved; candidate does not reimplement a second session state machine. | Controlled Forge-owned Chrome live canary remains. |
| A — effectful Work-attributed repository commands use durable Process (`7c789af8`) | **already fixed / subsumed** | Frozen Process Runtime/store remains canonical; candidate does not restore lightweight-effectful fallback. | Runtime live smoke after integration. |
| B1 — Requirement bootstrap duplicate admission in `runtime-tools.ts` | **fixed in this candidate** | New `requirement-authority.ts::admitRequirement` owns normalize/read/identity compare/reuse/conflict/create including concurrent-create reconciliation. `runtime-tools.ts` no longer imports/calls `createRequirement` for bootstrap. Architecture gate forbids regression. | Bun test suite on integrated exact tree. |
| B2 — explicit isolation could be routed to `direct_control` | **fixed in this candidate** | New `workspace-admission.ts` normalizes typed placement before Route Policy. `goal-workloop.ts` carries constraints through approval handoff and rejects forced direct when isolation is required. `route-policy.test.ts` adds regressions. No objective-language parsing is used. | Bun test suite plus managed-worktree real integration. |
| B2b — explicit `requiresUserApproval` could degrade to a generic blocked handoff | **fixed during integration** | Real focused execution showed Route Policy correctly selected `handoff_only`, but Access Policy could return `allowed`, causing the facade to omit `approvalAction` and report ordinary `blocked`. `goal-workloop.ts` now preserves Route Policy's `requiresApproval/waitForUser` decision as an actionable `approval_required` handoff without weakening destructive/strong-confirmation policy. | `route-policy.test.ts` passes on the final candidate. |
| B3 — helper/template exact-copy double authoring authority | **fixed in this candidate** | `scripts/sync-generated-authority.mjs` defines root authoring sources and verifies/synchronizes 67 packaged projections, including explicit fail-closed transforms for the five intentionally non-identical helper pairs and file modes. `prepack` and governed task gate check drift. Physical copies required for downstream packaging remain generated artifacts, not co-equal authoring sources. | Final duplicate report classifies retained physical copies; generated-authority gate must remain green. |
| C1 — Context Plane evidence closure | **fixed in this candidate** | Default `implementation + structural=auto` is now eligible for internal bounded evidence waves. Real source/CodeGraph relationships discovered but not materialized are tracked; any unresolved material relationship makes readiness `insufficient`, not merely `degraded`. New tests cover default source-reference expansion and bounded unresolved closure. | Bun context regressions on integrated tree; external CodeGraph availability remains environment-dependent. |
| C2 — Candidate B can fall back to user ControllerHome | **fixed in this candidate** | Verification snapshot owns an explicit isolated ControllerHome at `.git/forge-candidate-controller`. Persisted-check → sidecar → controller-check passes it explicitly after generic host-private env sanitization. `work-validation-reconciler.test.ts` proves it is outside source status. `check-runner-provenance.test.ts` proves host ControllerHome/writer authority does not leak and Candidate writes remain in the isolated home on both crash and timeout. | **live-only acceptance remaining** for a real Known-good A → Candidate B external API/CLI fault matrix, Recovery/runtime rotation and rollback proof. |
| C2b — compiled Candidate manifest dropped Process/sidecar identity when reloaded by the parent | **fixed in this candidate** | Frozen materialization wrote Process Runner, Check Runner, Browser hosts, plugin sidecar, external probe, CodeGraph and Controller UI identities, but `loadRuntimeReleaseManifest()` did not parse most of them. The parent therefore reconstructed a partial `StagedRuntimeRelease` and could skip component validation. Candidate now has one typed manifest parser for all known component identity pairs, `requireCompleteCompiledRuntimeReleaseManifest()` closes the compiled execution surface, staging reconciles manifest/receipt/source identity, and `assertRuntimeReleaseFiles()` validates physical paths, manifest hash, component hashes, directory hashes and execute bits. Regression source covers missing Process Runner authority, byte tampering and non-executable components while retaining unknown-future-field compatibility. | Run Bun regressions on the integrated exact tree; live activation/rollback remains parent acceptance. |
| C3 — second whole-repo authority/dead-code pass | **fixed in this candidate** | Second pass extracted Requirement bootstrap, repository Work admission/placement, retained-cancelled resume, and WorkHandle repair from transport-owned policy. No duplicate finalizer was found: `execution-tools.ts::finalizeWork` is the only Work finalization orchestrator; moving it solely for LOC would not eliminate an authority. No second Browser session or Process persistence state machine found. | Re-run architecture/static gates on exact final commit. |
| C3a — Work verification check registry drifted from the Work checkout | **fixed during integration** | Real Forge integration proved `rh_context`/Process could see a candidate-added `package:check:generated-authority` while `rh_work verify` classified it against canonical/main before resolving the Work checkout. `runFacadeVerify` now resolves the Work-bound checkout first, enumerates checks from that checkout, and fails closed as infrastructure if the checkout is unavailable. Independent repair checkpoint `0993b5194f3e5b6c09a3745218a9500ca83ce2c2` passed focused/type/architecture validation before its logical two-file delta was integrated into this candidate. | `rh-work-requirement-bootstrap.test.ts` reproduces a check added after Work admission and passes through the actual `rh_work verify` facade. |
| C3b — tracked shebang entrypoints lacked executable mode | **fixed in this candidate** | Exact final static-gate review found four tracked shebang entrypoints (`evaluation/run.ts`, `scripts/benchmark-work-efficiency.ts`, `scripts/check-stable-baseline.ts`, `tests/live/browser-runtime-v3.e2e.ts`) at mode `100644`. This pre-existed the freeze, but `check:executable-modes` is a source-controlled release gate and correctly rejected it. Candidate changes only the Git executable bit to `100755`; no startup auto-repair or fallback was added. | `check:executable-modes` must be green on the final exact tree. |
| C3c — new repository Work admission regression was absent from test governance | **fixed during integration** | The first real `package:check:main` correctly rejected `tests/runtime/repository-work-admission.test.ts` as undeclared. `tests/test-manifest.v1.json` now registers it as `module=routing`, `resource=pure`; governance validates 137 test files. No bypass or manifest weakening was added. | Final `package:check:main` must stay green. |
| D1 `hnd-1787883580872` — stale saved native-tab identity recurrence | **live-only acceptance remaining** | Source replacement/provenance repair was already in frozen main and remains canonical. No source regression found in second pass. | Forge-owned controlled Chrome tab/window canary after local integration only; never adopt user-owned tab by index. |
| D2 `hnd-1787884069199` — successor durable Work dispatch/conversation identity | **already fixed / subsumed** | Frozen `controller-round-relay.ts` persists relay state in control-plane SQLite, requires exact Work/controller claim, and `claimStalledControllerRoundRelays` reopens abandoned/stalled claimed rounds. `control-plane-hardening.test.ts` proves a dispatched round is acknowledged only after exact Work claim and a claimed abandoned round returns to dispatching; `autonomous-continuation-lifecycle.test.ts` covers abandoned release and explicit relaunch of the same fenced chain. ChatGPT Work binding tests refuse silent conversation rebind. | **live-only acceptance remaining** for actual external ChatGPT/browser continuation after runtime restart. |
| D3 `schedule-failure-36ee7908ded1b69bfb6d` — `domcontentloaded` timeout | **fixed in this candidate** | `browser-macos-bridge.ts` now emits bounded/classified `PLUGIN_BROWSER_LOAD_STATE_TIMEOUT`. `browser-adapter.ts` permits post-dispatch retry only for replay-safe observations on an existing resource; mutation/new-resource transient failures become non-retryable `PLUGIN_BROWSER_MUTATION_OUTCOME_UNKNOWN`. Browser regressions cover classification/replay policy. | Controlled Chrome live canary after integration. |
| E — frozen main not Stable / old Source Baseline / failed Stable receipt | **live-only acceptance remaining** | These are control-plane/runtime facts, not reasons to mutate offline source or falsify Stable state. Candidate does not switch Source/Stable Baseline. | Parent/local integration must perform exact-content gates, A/B verification, activation canaries and rollback proof before deciding Stable. |
| E — 26 disabled historical schedules | **intentional retained compatibility** | Historical occurrence evidence is intentionally not deleted merely to reduce counts. No second scheduler/store was introduced. | Any future terminal-schedule GC must preserve history and is outside this functional freeze. |
| Offline sandbox lacked Bun/project dependencies | **external blocker resolved by managed-worktree integration** | The supplied offline sandbox could not run Bun gates, so it correctly did not claim them. The isolated local integration environment subsequently ran real Bun/type/focused/main/release checks. | Final exact-content main → release sequence remains mandatory after the last documentation/source change. |
| One focused batch lost its Process result across a Forge Runtime restart | **external infrastructure interruption; recovered** | Process Runtime returned `PROCESS_RESULT_UNAVAILABLE_AFTER_RUNTIME_RESTART` and explicitly did not replay the command. The interrupted files were rerun after Runtime recovery and completed green; no Candidate acceptance was inferred from the interrupted run. | Preserve the interruption as infrastructure evidence; final gates must use terminal receipts. |

## Intentional retained compatibility boundaries

- Browser legacy session JSON: import/no-context compatibility only; canonical production persistence remains `BrowserSessionAuthority` in control-plane SQLite.
- Controller disposition compatibility capability (`controller.disposition:*`): parser/transport compatibility only; relay lifecycle stays in `controller-round-relay.ts`.
- Packaged helper/reference/template physical files: deterministic distribution projections required by downstream project templates; generator + drift gate make them non-authoritative.
- Legacy Edit-session start surface: translates to canonical repository Work admission before Edit Session ownership begins.

## False positives closed by source review

- **“Large file implies duplicate authority.”** False positive as a general rule. `execution-tools.ts` remains large, but the second pass found one finalization orchestrator rather than two competing finalizers. No LOC-only move is made.
- **“Context Plane still has only a fixed shallow retrieval.”** False positive for the frozen source. Multi-wave expansion, source-reference navigation, CodeGraph/semantic context, coverage/readiness accounting and cache already existed. Candidate fixes the default implementation eligibility/readiness closure gap instead of replacing the subsystem.
- **“Browser needs another watchdog/session store.”** False positive. Existing BrowserSessionAuthority is the canonical persistence/lifecycle source; the residual defect was retry/outcome classification, fixed inside the existing authority/adaptor design.
