---
id: "ISS-20260802-27931A"
kind: "governance"
status: "launch_blocked"
updated_at: "2026-08-02T06:25:19.727Z"
source: "repo-harness-controller-v8"
---

# 主服务不可用时仍能远程恢复

保留为独立的 Recovery 用户需求。源码继续位于 monorepo，但安装后使用自包含不可变发布、独立 launchd、独立状态、独立公网入口和独立 tunnel，不依赖主 Gateway、Daemon、active runtime 或 worktree。实现由主运行时稳定性需求统一定义边界。

## Goals

- Preserve monorepo source ownership for Supervisor and standalone Recovery.
- Install Recovery Gateway and Watchdog as self-contained immutable versioned artifacts with atomic activation.
- Keep Recovery Gateway, Recovery Watchdog and recovery tunnel as separate OS service units from Primary runtime services.
- Persist Recovery Watchdog state and prevent repeated noisy degraded loops or repeated rollback after watchdog restart.
- Probe and repair the dedicated Recovery public endpoint independently from the primary MCP endpoint.
- Remain usable when the repository checkout, Primary Gateway, Controller Daemon and active runtime are unavailable.

## Non-goals

- Do not make Recovery depend on the primary Gateway, Controller Daemon, active runtime instance, repository worktree or Bun at runtime.
- Do not let Supervisor supervise Recovery Gateway or Recovery Watchdog; launchd/systemd remains their common lower-level authority.
- Do not allow arbitrary shell execution or let Recovery mint/write primary runtime authority.
- Do not preserve or extend the old long-lived blue/green slot model for compatibility.
- Do not create a separate Recovery Git repository.

## Acceptance Criteria

- [ ] Recovery source remains under the repo-harness repository and is built from a clean canonical revision.
- [ ] Installed Recovery artifacts live under versioned immutable release directories and activate atomically.
- [ ] Recovery Gateway and Recovery Watchdog cold-start successfully without the repository worktree, primary Gateway, Controller Daemon or active runtime.
- [ ] Both Recovery services restart after abnormal exit and remain stopped after an explicit authorized unload.
- [ ] A dedicated recovery cloudflared service exposes only the Recovery endpoint and is independently repairable.
- [ ] Watchdog separately evaluates local primary state, primary public endpoint, local Recovery Gateway and public Recovery endpoint.
- [ ] Watchdog state survives restart and rollback/tunnel-repair actions remain idempotent, identity-scoped and rate-limited.
- [ ] Recovery cannot mutate primary runtime authority and exposes only bounded verify/restart/rollback/cleanup operations.
- [ ] Focused unit, smoke, launchd template, interrupted-install and tunnel-failure tests pass.

## GitHub

- Not published.

## Tasks

### T1 — Define recovery authority and failure-domain contract

- Status: `blocked`
- Objective: Add or update architecture decisions documenting monorepo source ownership, immutable installed artifacts, OS-level service ownership, and the four distinct monitoring layers.
- Depends on: none
- Allowed paths: `docs/architecture/**`, `docs/operations/**`, `docs/wiki/**`
- Checks: not defined
- Execution hint: selected at runtime

### T2 — Version and atomically activate Recovery artifacts

- Status: `planned`
- Objective: Replace flat in-place Recovery binary installation with immutable release directories, manifest verification, and atomic current activation while retaining bounded cleanup.
- Depends on: `T1`
- Allowed paths: `scripts/install-standalone-recovery.ts`, `scripts/load-standalone-recovery.sh`, `src/runtime/standalone-recovery/**`, `tests/**`
- Checks: not defined
- Execution hint: selected at runtime

### T3 — Persist and harden Recovery Watchdog state

- Status: `planned`
- Objective: Persist failure counters, first-failure timestamps, rollback-used state, tunnel cooldowns, and last decision across Watchdog restarts; debounce repeated degraded logs.
- Depends on: `T1`
- Allowed paths: `src/runtime/standalone-recovery/**`, `tests/**`
- Checks: not defined
- Execution hint: selected at runtime

### T4 — Add dedicated Recovery endpoint and tunnel supervision

- Status: `planned`
- Objective: Separate primary and Recovery public endpoint configuration, generate a dedicated cloudflared service contract, probe the local/public Recovery endpoint, and repair only the failed tunnel.
- Depends on: `T3`
- Allowed paths: `src/runtime/standalone-recovery/**`, `scripts/install-standalone-recovery.ts`, `scripts/load-standalone-recovery.sh`, `docs/operations/**`, `tests/**`
- Checks: not defined
- Execution hint: selected at runtime

### T5 — Verify self-recovery and failure isolation

- Status: `planned`
- Objective: Add focused tests and smoke drills covering Supervisor failure, Recovery Gateway failure, Watchdog failure, primary tunnel failure, Recovery tunnel failure, and concurrent operation deferral.
- Depends on: `T2`, `T4`
- Allowed paths: `tests/**`, `scripts/smoke-runtime-recovery.ts`, `docs/operations/**`
- Checks: `bun run check:type`; `bun test tests/runtime/standalone-recovery.test.ts tests/runtime/stable-supervisor-hardening.test.ts`; `bun run check:runtime-architecture`
- Execution hint: selected at runtime
- Evidence (2026-08-02): P0 recovery hardening is implemented in the Supervisor/standalone Recovery path. Gateway absence is now treated as an incomplete active pair and recovery can start a verified registered release explicitly selected by `targetReleasePath`; Recovery refuses directory/PID/port-only candidates and requires manifest, Controller Home, config revision/hash, and writer-fencing attestation. Focused checks passed: 77 tests, typecheck, and runtime architecture check. Full T5 smoke and launchd drills remain pending.

Live evidence (2026-08-02): an explicit controlled restart request was attempted without rollout or release promotion. Supervisor/Ingress restarted and the Daemon reached `ready`, but the installed `183c490...` Gateway failed readiness because its bundled runtime fell back to spawning the source-checkout `src/runtime/execution/process-runtime/process-runner-entry.ts` (`EACCES`). The source fix is already present in `2ff6eda64` and the focused process/supervisor suite passes (100 tests); activating a release built from that revision remains a separate gated operation.

## Related Artifacts

- `ISS-20260802-539E7F T6`
- `existing standalone Recovery implementation`
- `dedicated Recovery tunnel and watchdog requirements`
