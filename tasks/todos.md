# Deferred Goal Ledger

> **Status**: Backlog
> **Updated**: 2026-08-24 14:45
> **Scope**: Medium/long-term goals deferred from active plan execution

Current plan tasks live in the active plan's `## Task Breakdown`.
Do not duplicate that execution checklist here. Record only work intentionally deferred beyond this slice, with the tradeoff and revisit trigger.

## Deferred Goals

| Goal | Why Deferred | Tradeoff | Revisit Trigger |
|------|--------------|----------|-----------------|
| Persist a transaction manifest on partial `--experimental-ts-apply` failure (record applied + failed ops, or auto reverse-rollback on failure) | 0.6.1 scoped to the success-then-undo recovery path; crash/interrupt recovery is a larger applicator change | Today a mid-apply failure leaves applied ops with backups but no manifest, so the new `adopt rollback` cannot unwind them — only fully-successful applies are recoverable | When `--experimental-ts-apply` graduates toward default (0.7.x parity work) or a real partial-failure recovery need surfaces |
| Stop `restore_backup` rollback from leaving a stray `.bak` in the default `BACKUP_ROOT`; make `check-tarball-install-smoke.sh` resilient to an offline `bun add` (cache `commander`) | LOW-impact polish, no correctness effect | Minor backup litter during restore; the release-gate smoke needs network/cache for one dep | When touching fs-transaction backups again, or wiring the smoke into a fully-offline CI lane |
| Close the 2026-08-11 Forge residual runtime/execution issues (`tasks/notes/20260811-forge-residual-runtime-issues.notes.md`) | Active optimization is intentionally split across sessions; one stable ledger prevents rediscovery and duplicate fixes | Until closed, Process fixed tax, active-index O(N), stale MCP schema, legacy shell plugin false locks, and cold Git cost remain measurable residuals | Re-read the ledger at the start of every Forge optimization session; update each item with commit + benchmark evidence when fixed |
| Add a stable business-goal identity (`goalKey` / authoritative scope identity) across intentionally isolated WorkContracts | `d2e6c845` now prevents two non-isolated Works from writing the same checkout and preserves ChatGPT schedule conversation affinity; inferring semantic equality across legitimate isolated slices is a broader portfolio/goal-identity design change | An explicit isolated Work can still duplicate an existing product goal if a caller invents a new scope, but it can no longer become a second writer of the canonical checkout | When changing Plan scope semantics, Goal/Portfolio identity, or cross-worktree Work admission |
| Fix lightweight Process Runtime registration and request-id reconciliation: `repository_command_execute` can return `lightweight:<uuid>` with `status=running`, while immediate `process_get` / `process_wait` returns `PROCESS_NOT_FOUND`; replaying the exact same `request_id` can launch a different lightweight id, and `apply_mode=async` still routes to the same lightweight path | Reproduced repeatedly on 2026-08-20 while validating `investment-decision-system`; the product work had a safe validation workaround and should not be blocked by an unrelated Forge kernel repair | Long-running checks cannot be reliably joined, callers may accidentally re-execute idempotent-looking commands, and expected background-test parallelism collapses into ad-hoc reconciliation | Next Process Runtime / lightweight-handle optimization pass; add a regression proving every returned handle is queryable before response and request-id replay returns the original process/result |
| Preserve at least one independently attested known-good whole-Runtime release and its bound SQLite backup through release garbage collection | During the 2026-08-22 package Runtime exit-127 incident, `known-good.json` referenced releases whose manifests had already been deleted, while `current.previous` was not independently attested; automatic rollback and direct known-good activation were both unavailable | Recovery can restart a broken active release but may have no usable rollback target, forcing a fresh source rebuild during an outage | Next release-retention or Recovery rollback change; add failure injection that performs garbage collection, breaks the active release, and proves an attested whole-release rollback still succeeds |
| Fail fast when `scripts/migrate-project-template.sh` resolves its upstream script to itself | The required self-migration dry-run recursively `exec`ed the same adapter when `AGENTIC_DEV_ROOT` or the installed Skill symlink resolved to the current Forge checkout, consuming CPU indefinitely with no output | The migration gate cannot complete in the self-host checkout and can leave a runaway shell until manually stopped | Next workflow migration/installer change; compare canonical paths before `exec` and add a self-host recursion regression |
