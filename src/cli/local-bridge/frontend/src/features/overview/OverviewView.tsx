import type{ConsoleData,Dict}from'../../types';import{CommandBar}from'../../shell/CommandBar';import{StatusText}from'../../components/StatusText';import{SectionHeader}from'../../components/SectionHeader';import{compact}from'../../lib/format';
function attentionKey(h:Dict){return`${String(h.title??'')}:${String(h.reason??'')}`}
export function OverviewView({data,busy,onRefresh}:{data:ConsoleData;busy:boolean;onRefresh:()=>void}){const cc=data.commandCenter,a=data.automations.summary,w=data.work,p=cc.pluginSummary??{},repos=cc.repositories??[],readiness=cc.readiness??{};const unique=[...(cc.handoffs??[])].filter((h,i,all)=>all.findIndex(x=>attentionKey(x)===attentionKey(h))===i);const workAttention=w.waitingForUserCount??0;const attention=[...unique.slice(0,3),...(workAttention?[{title:'Persistent work needs attention',reason:`${workAttention} 项长期目标需要确认`,statusLabel:'Review',tone:'warning'}]:[]),...((p.needsAttention??0)?[{title:'Capabilities need attention',reason:`${p.needsAttention} 项能力需要配置或检查`,statusLabel:'Inspect',tone:'warning'}]:[])].slice(0,4)as Dict[];return <><CommandBar eyebrow="FORGE" title="Overview" description="长期配置与系统可用性。执行结果和通知继续由 ChatGPT 承担。" refreshedAt={data.generatedAt} busy={busy} onRefresh={onRefresh}/><div className="metric-strip"><a href="#/automations"><span>Automations</span><strong>{a.enabled}</strong><small>{a.paused?`${a.paused} paused`:'enabled'}</small></a><a href="#/capabilities"><span>Capabilities</span><strong>{p.ready??0}/{p.total??(cc.plugins??[]).length}</strong><small>ready</small></a><a href="#/repositories"><span>Repositories</span><strong>{repos.length}</strong><small>registered</small></a><a href="#/work"><span>Persistent work</span><strong>{w.activeRequirementCount??0}</strong><small>{w.waitingForUserCount??0} waiting</small></a></div><section className="page-section"><SectionHeader title="Needs attention" meta={attention.length?`${attention.length} actionable groups`:'All clear'}/>{attention.length?<div className="attention-list">{attention.map((h,i)=><div className="attention-row" key={`${String(h.title)}-${i}`}><span className="attention-index">{String(i+1).padStart(2,'0')}</span><div><strong>{String(h.title??'需要处理')}</strong><p>{compact(String(h.reason??'请在 ChatGPT 中确认'),120)}</p></div><StatusText label={String(h.statusLabel??'Review')} tone={String(h.tone??'warning')}/></div>)}</div>:<div className="quiet-empty"><StatusText label={String(readiness.label??readiness.headline??'Ready')} tone={String(readiness.state??'success')}/><p>当前没有需要你在控制台处理的配置问题。</p></div>}</section><section className="page-section"><SectionHeader title="Configured work" meta="只显示持久化目标，不展示执行流水"/><div className="compact-work-list">{(w.requirements??[]).filter(x=>x.state!=='done'&&x.state!=='cancelled').slice(0,5).map(x=><a href="#/work" key={x.requirementId}><div><strong>{x.title}</strong><p>{compact(x.outcome,100)}</p></div><StatusText label={x.state==='waiting_for_user'?'Waiting':x.state==='planned'?'Planned':'Active'} tone={x.state}/></a>)}</div></section></>}
