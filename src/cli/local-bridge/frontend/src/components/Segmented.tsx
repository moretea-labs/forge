export interface Segment<T extends string>{id:T;label:string;count?:number}export function Segmented<T extends string>({items,value,onChange}:{items:Segment<T>[];value:T;onChange:(v:T)=>void}){return <div className="segmented" role="tablist">{items.map(i=><button key={i.id} role="tab" aria-selected={value===i.id} className={value===i.id?'selected':''} onClick={()=>onChange(i.id)}>{i.label}{i.count!==undefined&&<span>{i.count}</span>}</button>)}</div>}
